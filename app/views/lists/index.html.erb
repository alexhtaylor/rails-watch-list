<center><table>
  <thead>
    <tr>
      <th class="header">Lists</th>
    </tr>
  </thead>
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td class="red"><%= list.name %></td>
        <td><%= link_to "details", list_path(list) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<h2><%= link_to "create a new list", new_list_path %></h2></center>
